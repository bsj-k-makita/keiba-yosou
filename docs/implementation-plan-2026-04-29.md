# 実施計画書（2026-04-29）

## 目的
- 既存の「能力 + 条件 + 格 + 距離」評価に追加した文脈補正（血統・枠順バイアス・陣営・傾向・前後傾差・不利恩恵）を、実データ投入とUI可視化まで接続する。

## スコープ
- 対象:
  - `scripts/` の netkeiba 取得処理
  - `src/lib/race-data/` の変換処理
  - `src/components/race/` の表示処理
  - `docs/` の仕様更新
- 非対象:
  - 新規外部DB構築
  - 高コストな全履歴レース回顧 NLP

## 実装フェーズ

### Phase 1: 入力データの拡張（最優先）
- [x] 出馬表から `sex` / `age` / `trainer` / `bodyWeightKg` を抽出
- [x] 可能な範囲で血統文字列（`sireName` / `damSireName`）を取り込み
- [x] `analysisJson` 互換形式で保存されることを確認

### Phase 2: 評価結果の可視化（運用必須）
- [x] 馬カードに文脈補正内訳（血統/枠/陣営/傾向/前後傾差/不利恩恵）を表示
- [x] 一覧テーブルで文脈補正の合計を確認可能にする
- [x] 「最終点の算出根拠」が UI 上で追える状態にする

### Phase 3: 回顧データ連携（段階導入）
- [x] `pastRuns` に `tripTrouble01` / `tripBenefit01` を投入する入力経路を決める
- [x] 半自動入力（CSV/手入力）フォーマットを定義
- [x] 反映チェック用のサンプルレースを固定化

### Phase 4: 運用検証（回帰確認）
- [x] `npm run build` を通す
- [x] 既存レース JSON で移行破壊がないことを確認
- [x] 追加項目未投入時に従来挙動（中立）を保つことを確認

## 進行ログ
- 2026-04-29:
  - 文脈補正レイヤー（計算本体）は実装済み
  - 本計画書を作成
  - Phase 1/2 を実装完了（取得拡張 + UI可視化）
  - Phase 3 を実装完了（CSV投入スクリプト + 仕様書 + テンプレ + サンプルレース固定）
  - Phase 4 を実施完了（build通過、既存JSON互換確認、未投入中立の確認）

